int main6()
{
	int num1 = 0;
	scanf("%d", &num1);      //  & 
	//%d -> 정수
	//%c -> 문자
	//%s -> 문자열
	//%o -> 8진수
	//%x -> 16진수 
	//%lf -> 실수

	printf("\"멍멍\"");
	//\" -> 큰 따옴표 
	//\\ -> 역슬래쉬
	//\t -> 수평 탭
	//\b -> 백 스패이스
	//\a -> 경고음

	//%d -> 정수 
	//%o -> 8진수
	//%x -> 16진수  
	//%f -> 실수  
	//%c -> 문자  
	//%s ->문자열
	//%p ->포인터 주소 값
	//%숫자d  -> 필드폭 확보
	return 0;
}